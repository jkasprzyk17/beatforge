# Copy values from here into Vercel → Project → Settings → Environment Variables
# (do NOT commit the real values to git)

# Your AWS backend URL (EC2 / ECS / Elastic Beanstalk / etc.)
VITE_API_URL=https://api.your-domain.com
